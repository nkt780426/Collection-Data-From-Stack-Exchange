1. Cấu hình aws cli với tài khoản IAM
2. Tạo cụm k8s 1 master node và nhiều 2 worker nodes với free tier đồng thời map nó với dns free
3. Cài apache spark sử dụng helm
4. Tạo các resources k8s
    - Có các deployment nào, configmap + secret, service nào
    - Tạo ingress controller nginx, cấu hình ingress.yaml (theo path)
